/* timestamps is a an array of timestamp objects */
exports.cycletime = function (timestamps, err) {
  // sort'em

  // calculate the interval between the earliest and latest
  return;
}

/* timestamps is a an array of timestamp objects */
exports.meantime = function (timestamps, err) {
  // sort'em

  // calculate
  return;
}

exports.overhead = function  (timestamps, err) {
  // need to pull calc off whiteboard
  return;
}